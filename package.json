{
  "name": "phisher-repo",
  "version": "1.0.0",
  "description": "SkyStream plugins repository",
  "private": true,
  "scripts": {
    "deploy": "skystream deploy -u https://raw.githubusercontent.com/USER_NAME/phisher-repo/main/"
  },
  "devDependencies": {
    "skystream-extractors": "latest"
  }
}
